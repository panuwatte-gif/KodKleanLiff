# KodKlean LIFF — UI Mockup (Phase 1)

## ภาพรวม
- LIFF บันทึกค่าใช้จ่ายฝั่งซื้อ + ลงบัญชี (mockup ยังไม่เชื่อม backend/Supabase)
- ผู้ใช้เขียนโค้ดไม่เป็น → แยกไฟล์ให้แก้ง่าย พังยาก, คอมเมนต์หัวไฟล์ทุกไฟล์
- สไตล์: น่ารักมาก สีสันสดใสหลากสี การ์ดกดง่ายแบบแอปสั่งอาหาร ตัวหนังสือใหญ่ (≥16px) ปุ่ม ≥48px ฟอนต์ Noto Sans Thai
- ตัวการ์ตูน: อาเฮีย (ชายใส่แว่น) + แม่พัน (หญิง) จับคู่ "วัยเดียวกัน" เท่านั้น สลับวัยไปตามหน้า **ห้ามใช้วัยชรา** ห้ามซ้ำแบบ ต้องเห็นชัด ไม่ใช่ตัวจิ๋ว
- mascot ลูกเจี๊ยบ (assets/mascots) ใช้ประกอบหน้า + แทนไอคอน / ไอคอน 3D ใช้ assets/icons
- โลโก้ร้าน supplier: assets/logos / โลโก้แบรนด์ในเครือ: assets/brandlogos / รูปสินค้า: assets/products

## โครงไฟล์
- `KodKlean_LIFF.dc.html` — UI ทั้งหมด (ทุกหน้า + logic)
- `data.js` — ข้อมูล mock ทั้งหมด (ร้าน/สินค้า/รายการซื้อ/กราฟ) แก้ข้อมูลที่นี่ที่เดียว
- `kk-helpers.js` — โมดูลช่วย: ปฏิทินไทย, กฎคัดกรองตั้งต้น+ตัวผูกสูตรบัญชี (ล็อก), CSV/ZIP
- `config.js` — ค่าเชื่อมระบบจริง (LIFF_ID/Supabase) · `supabase/` — schema อ้างอิง
- ข้อมูลที่ผู้ใช้แก้ในแอป เก็บ localStorage key `kk_liff_v1` · กฎคัดกรองเก็บใน fin_rules ด้วยเมื่อเชื่อมจริง

## กติกาสำคัญจาก prompt
- ห้ามโชว์รหัส route (1a/1b/2a/2b) ให้ผู้ใช้เห็น — แสดงข้อความเต็มเสมอ
- Dashboard: เงินออกเพิ่ม = เรื่องไม่ดี ห้ามใช้สี/ลูกศรเชิงบวก, ห้ามแสดงรายได้/กำไร
- never block ตอนบันทึกร่าง / block เฉพาะตอนยืนยัน+เซ็น
- งบ 2 ชุด: ส่วนตัว (ทุกรายการ) vs จำลองนิติ (เฉพาะเอกสารครบ) + หมายเหตุ "ต้นทุนขายจริงรอเชื่อม App_stock"
